version: '3.4'

x-app-override:
  &app-override
  volumes:
    - ${APP_BASE:-.}:${APP_DIR}/

services:
  app:
    <<: *app-override

  app-production:
    <<: *app-override

  shell-linter:
    volumes:
      - ${APP_BASE:-.}:/mnt/

